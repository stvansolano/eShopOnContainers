#  docker-compose -f "src/docker-compose.catalog-api.yml" up --env-file ./config/.env.dev -d
version: '3.4'

services:
  sqldata:
    image: mcr.microsoft.com/mssql/server:2017-latest 
    environment:
      - SA_PASSWORD=$ESHOP_SQL_SERVER_SA_USER
      - ACCEPT_EULA=$ESHOP_SQL_SERVER_SA_PASSWORD
    ports:
      - "5433:1433"
    volumes:
      - eshop-sqldata:/var/opt/mssql

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: ${ESHOP_SERVICE_BUS_USERNAME}
      RABBITMQ_DEFAULT_PASS: ${ESHOP_SERVICE_BUS_PASSWORD}
    ports:
      - "15672:15672"
      - "5672:5672"

  catalog-api:
    build:
      dockerfile: Services/Catalog/Catalog.API/Dockerfile
      context: .
    environment:
      - ConnectionString=Server=sqldata,5433;Database=Microsoft.eShopOnContainers.Services.CatalogDb;User Id=sa;Password=Pass@word
      - PicBaseUrl=${ESHOP_STORAGE_CATALOG_URL}
      - EventBusConnection=rabbitmq
      - EventBusUserName=${ESHOP_SERVICE_BUS_USERNAME}
      - EventBusPassword=${ESHOP_SERVICE_BUS_PASSWORD}
      - AzureStorageAccountName=${ESHOP_AZURE_STORAGE_CATALOG_NAME}
      - AzureStorageAccountKey=${ESHOP_AZURE_STORAGE_CATALOG_KEY}
      - UseCustomizationData=True
      - AzureServiceBusEnabled=False
      - AzureStorageEnabled=False
      - ApplicationInsights__InstrumentationKey=${INSTRUMENTATION_KEY}
      - OrchestratorType=${ORCHESTRATOR_TYPE}
      - GRPC_PORT=81
      - PORT=80
      - PATH_BASE=/catalog-api
    ports:
      - "5101:80"
      - "9101:81"
    depends_on:
      - rabbitmq
      - sqldata
    links:
      - rabbitmq
      - sqldata

  webstatus:
    #image: ${REGISTRY:-eshop}/webstatus:${PLATFORM:-linux}-${TAG:-latest}
    build:
      context: . #src
      dockerfile: Web/WebStatus/Dockerfile
    depends_on:
      - rabbitmq
      - sqldata
      - catalog-api
  tests:
    build:
      context: . #src
      dockerfile: Services/Catalog/Catalog.FunctionalTests/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://0.0.0.0:80
    ports:
      - "5108:80"
    depends_on:
      - rabbitmq
      - sqldata
      - catalog-api
    links:
      - rabbitmq
      - sqldata
      - catalog-api

volumes:
  eshop-sqldata:
    external: false